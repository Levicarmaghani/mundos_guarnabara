# Crie um programa que leia o ano de nascimento de 7 pessoas. 
# No final mostres quantas pessoas ainda não atingiram a maioridade 
# Quantos ja são maiores.

from datetime import date
maior = 0
menor = 0

atual = date.today().year
for pessoa in range(1, 8):
    print(f'Pessoa {pessoa}º')
    nascimento = int(input(f" Digita o ano do seu nascimento: "))
    idade = atual - nascimento
    if idade <= 18:
       menor += 1
       print(f"Sua idade {idade} Voce ainda é de menor ")
    else:
        maior += 1
        print(f"Sua {idade} Voce já é de maior: ")
print(f"Total de pessoa de maiores são {maior}\nTota de pessoa de menor são {menor}")