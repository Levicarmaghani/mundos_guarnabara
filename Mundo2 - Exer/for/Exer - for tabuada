# Mostre a tabuada de um número que o ususario escolher com o FOR
from time import sleep

usuario = int(input("Digite um número para ver sua tabuada: "))

for tabuada in range(0, 11):
    resultado = usuario * tabuada
    sleep (1)
    print(f"{usuario} X {tabuada} = {resultado}")
    