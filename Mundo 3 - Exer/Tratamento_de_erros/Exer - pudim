# Crie um codigo em python que teste se o site pudim está acessivel pelo computador usado 

# Importando a biblioteca para testar 
import urllib
from urllib import request

# Criando a função para verificar a URL
import os
import urllib.error
os.system('cls')  # Limpa o terminal no Windows
 
try:
    site = urllib.request.urlopen('https://www.pudim.com.br')
except (urllib.error.URLError):
    print('Esse site está sem conexão')
else:
    print('Conexão com sucesso')

