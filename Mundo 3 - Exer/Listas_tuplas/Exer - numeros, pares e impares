# Crie um programa que vai ler varios números e colocar em uma lista
# Depois disso crie duas lista extra. ums que vsi conter so os valores pares 
# E a outra valores impares digitados respectivamente
# No final mostre as tres listas geradas
lista = [[],[]]

while True:
    numeros = int(input("Digite um valor inteiro númerico: "))
    if numeros % 2  == 0:
        lista[0].append(numeros)
    else:
        lista[1].append(numeros)
    continuar = str(input("Deseja continuar (S/N) ")).strip().upper()
    while continuar not in "SN":
        print("DIGITO INVALIDO TENTE NOVAMENTE")
        continuar = str(input("Deseja continuar (S/N) ")).strip().upper()
    if continuar == "N":
        break
    if continuar == "S":
        continue
print(f"""
Sua lista {lista}
Sua lista de números pares {lista[0]}
Sua lista de números impares {lista[1]}
""")

