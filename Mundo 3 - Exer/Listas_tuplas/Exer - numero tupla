# Crie um programa que vai gera 5 números aleatorios e colocar em uma tupla 
# Depois disso mostre uma listagem de números gerados e tambem indiquem o menor e o maior
# valor que estão na dupla 
from random import randint
numeroaleatorio = (randint(0, 10), randint(0, 10), 
    randint(0, 10), randint(0, 10), randint(0, 10))

for n in numeroaleatorio:
    print(f"{n}", end = ' ') # O end imperde que pula de linha 
print(f"""
O maior número sorteado foi de {max(numeroaleatorio)} 
O menor número sorteado foi de {min(numeroaleatorio)} """)
# O max sempre ira mostra o numero maior da tupla
# o min sempre ira mostrar o número menor 