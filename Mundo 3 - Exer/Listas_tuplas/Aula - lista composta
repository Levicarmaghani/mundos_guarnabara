"""teste = list()
teste.append('Levi')
teste.append(29)
galera = list()
galera.append(teste[:])
teste[0] = 'Laura'
teste[1] = 20
galera.append(teste[:])"""

# galera = [['Akaza', 30], ['Renkogu', 30], ['luffi', 30], ['Tanjiro', 26]]
# for p in galera:
#    print(f" Seu nome {p[0]} sua idade {p[1]}")

galera = list()
dado = list()
maior = 0
menor = 0
for c in range(0, 3):
    dado.append(str(input("Nome: ")))
    dado.append(int(input("idade: ")))

    # Aqui o codigo faz uma copia da lista, usando [:]criando uma copia raza da lista e adiciona essa copia a lista galelra. a razão de usar o [:] é para garantir uma nova referencia direto para lista do dado 
    galera.append(dado[:])

    # Após adicionar os dados de uma pessoa em galera, a lista dado é "limpa" usando o método clear(). Isso é feito para garantir que, na próxima iteração do laço, dado comece vazia, pronta para armazenar as informações da próxima pessoa.
    dado.clear()
# para cada pessoa da lista galera
for pessoa in galera:
    if pessoa[1] >= 18:
        print(f" A pessoa {pessoa[0]} é maior de idade .")
        maior += 1
    else:
        print(f"Pessoa {pessoa[0]} é menor de idade ")
        menor += 1
print(f"Total de pessoas maiores de idade é de {maior} menor de idade {menor}")