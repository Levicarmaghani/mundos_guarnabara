# Crie um Programa que crie uma matriz de dimensão 3X3 e prencha com valores lidos pelo teclado
# No final mostre na tela com formatação correta 
# Mostre a soma de todos os valores digitados 
# A soma de todos os valores pares digitados 
# A soma de todos os valores da terceira coluna 
# O maior valor da segunda linha

# Definindo uma lista, dentro dessa lista tem outra lista com os valores 0
# A primeira lista a posição dela é 0  
lista = [[0, 0, 0],[0, 0, 0,],[0, 0, 0, ]] 

# Para cada linha percorra ela 3 vezes vai do (0, 2)
for linha in range(0, 3):
     # Para cada coluna percorra ela 3 vezes 
    for coluna in range(0, 3):
        lista[coluna][linha]= int(input(f"Digite um valor para posição [{linha}, {coluna}]"))
for linha in range(0, 3):
    for coluna in range(0, 3):
        print(f"[{lista[linha][coluna]:^5}]", end = '')
    print()
        
