# Crie um programa que tenha uma função fatoriaç()
# Vai receber dois paramentros: o primeiro que indique número a calcular eo outro chamado show, que será o #  valor lógico(opcional) indicando se será mostrado ou não na tela o processo de  cáculo do fatorial

# Função 
def fatorial():
    """
    - Função que retorna o fatorial
    - Pedimos o número para ao usuário retornando o valor 
    - Temos a função que da a opção mostrar a logica 
    """
    sequencia = list()
    numero = int(input("Digite o número: "))
    fator = 1  # Inicializa a variável fator como 1 (o valor de multiplicação inicial)
       
    # Cálculo do fatorial
    for valor in range(numero, 0, -1):
        fator *= valor  # Atualiza fator multiplicando-o pelo valor de cada iteração
        sequencia.append(str(valor))
    print(f'Fatorial de {numero} = {fator}')
        # Exibindo a sequencia do calculo 

    show = input("Deseja ver a logica: (S/N)").strip().upper()
    if show == "S":
        # A função join serve para ajuntar os elementos, como o x veio antes, ele colocou o x entre cada numero em sequencie, por utilmo temos o = e p fator que é o resultado 
        print(f" X " .join(sequencia), "=", fator)
    return fator

# Chama a função
resultado = fatorial()
help(fatorial)



     
 

        
  
