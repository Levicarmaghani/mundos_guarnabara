# Faça um programa que tenha uma função chamada ficha().
# Recebe dois parametros opcionais:
# 1) o nome de um jogador
# 2) quantos gols ele marcou.
# O programa devera ser capaz de mostrar a ficha do jogado. mesmo que algun dado não tinha sido informado corretamente
lista_principal = list()

def ficha():
    nome = str(input("Nome do jogador: "))
    # Se o teclado for so dado enter nome recebe jogador desconhecido
    if nome == "":
        nome = "Jogador desconhecido" 
    
    gols = input("Quantos gols feitos: ").strip()
    # Se o usuário não digitar gols, assume 0
    if gols == "":
        gols = 0
    else:
        gols = int(gols)

   
    # Adicionando nome e gols em minha lista_principal
    lista_principal.append((nome, gols))

    # Para cada valor da minha lista principal 
    for v in lista_principal:
        print(f" {v [0]} Voce fez {v[1]} gols")

    return lista_principal


ficha()

    